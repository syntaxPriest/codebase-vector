services:
  backend:
    build:
      context: ./backend
    ports:
      - "4112:4111"
    env_file:
      - ./backend/mastra/.env
    volumes:
      - ./backend/mastra:/app/mastra
      - /app/mastra/node_modules
    environment:
      - MASTRA_HOST=0.0.0.0
